# No runtime secrets required for local development.
# Contact form uses FormSubmit (public endpoint) — activate via email on first submit.
# Optional: override site URL for previews
# PUBLIC_SITE_URL=http://localhost:4321
